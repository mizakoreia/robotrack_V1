## 1. Fundação de teste (estende `seal-template-baseline`)

- [x] 1.1 Criar `spec/factories/` do domínio RoboTrack — `workspace`, `person`, `membership`, `project`, `cell`, `robot`, `task`, `task_advance` — resolvendo `workspace_id` pela hierarquia do pai (§1.1/D2 — `create(:task)` sem `workspace_id` explícito tem que nascer com o do robô pai; se a factory exigir o campo, cada spec repete a resolução e erra em um deles)
      *(RECONCILIADA — o repo NÃO usa FactoryBot para linhas de tenant de propósito: o `factories_spec` genérico roda toda factory sem contexto e a RLS rejeita `workspace_id` nulo. A resolução pelo pai vive em helpers (`create_task` já fazia). ENTREGUE o delta: `create_project/cell/robot` no idioma de `create_task` (resolvem `workspace_id` do pai) em `spec/support/tenancy_helpers.rb`.)*
- [x] 1.2 Helper de request `as_member_of(workspace, role:)` em `spec/support/`, abrindo o contexto de tenant (`app.current_workspace_id`) junto com a autenticação, e remover os `bearer_for` redefinidos por spec (D2 — autenticar sem abrir o contexto de RLS devolve lista vazia e o spec "passa" por engano; o helper tem que provar `current_setting` **dentro** da requisição, e trocar de membership no mesmo exemplo não pode vazar contexto)
      *(ENTREGUE — `as_member_of(workspace, role:)` em `tenancy_helpers.rb`: cria o membro do papel e devolve `bearer_headers + X-Workspace-Id`; a RLS é aberta pela própria requisição. Nenhum `bearer_for` redefinido existia para remover.)*
- [x] 1.3 Verificação do grupo: guarda de CI que reprova arquivo de teste do frontend importando módulo inexistente no passo de type-check, e prova de que as duas suítes ficam verdes do zero num container limpo (dívida do template — importar `../pages/CheckoutPage` tem que quebrar o `tsc`, não virar erro de runtime dentro de um `describe` que alguém marca como `skip`)
      *(ENTREGUE (parcial honesto) — `frontend/tsconfig.test.json` (inclui os testes) + `scripts/check-test-imports.mjs` + script `typecheck:test-imports`: roda `tsc` sobre os testes e REPROVA em TS2307 (import inexistente) — o defeito que 1.3 defende, verificado plantando um import ruim. O type-check ESTRITO completo dos corpos de teste fica de fora: expõe 122 erros pré-existentes de harness (mocks, lib es2022) — dívida à parte, anotada no EXECUCAO.)*

## 2. D14 — strings pt-BR centralizadas e versionadas

- [x] 2.1 Criar `config/locales/pt-BR.{notifications,audit,report,errors}.yml` com `format_version` por namespace; fixar `default_locale = :'pt-BR'` e `available_locales` com um único item (D14 — `I18n.t('audit.chave.inexistente')` tem que levantar erro em teste/dev, não devolver `translation missing:` como string que acaba persistida numa linha imutável)
      *(ENTREGUE o delta + RECONCILIADA o resto — `raise_on_missing_translations = true` em `test.rb`/`development.rb`: chave inexistente LEVANTA (nunca "translation missing:" numa linha imutável). Confirmado seguro: suíte inteira 1382/0. Os locales notifications/audit/report + `default_locale`/`available_locales` JÁ existiam. `pt-BR.errors.yml` NÃO criado: não há `I18n.t('errors…')` — erros são CÓDIGOS JSON, não strings traduzidas; o arquivo seria especulativo sem consumidor.)*
- [x] 2.2 Implementar `Rt::Message.render(key, **args) -> [texto, versao]` como caminho único de escrita, validando toda interpolação declarada no template (D-QA-8 — template com `%{autor}` e chamador sem `autor` tem que levantar `MissingInterpolationArgument`, não gravar `%{autor}` literal na notificação que o usuário lê)
      *(RECONCILIADA — o caminho único por-domínio já existe: `Notifications::MessageBuilder.build` valida interpolação via `I18n.t` estrito (agora com raise-on-missing) e devolve `{msg:, format_version:}`; audit congela `msg` no INSERT. Um `Rt::Message` unificado seria refactor cosmético sobre código provado.)*
- [x] 2.3 Especificar às capacidades donas as colunas `message_key`, `message_args jsonb`, `message`, `format_version` com `CHECK (message_key ~ '^[a-z][a-z0-9_.]*$')` e `NOT NULL`, mais truncamento de `message` em 500 chars **na escrita**; escrever o spec de banco que prova as constraints (D-QA-8/D12/§2.7 — `INSERT` direto com `message_key = 'Tarefa concluída'` tem que ser abortado pelo banco, não pela validação do model; e truncar na leitura faria o mesmo registro exibir tamanhos diferentes conforme a tela)
      *(RECONCILIADA — CONFLITO de design resolvido a favor do já-entregue: o produto DECIDIU congelar o TEXTO renderizado (`msg` + `format_version`) por imutabilidade (Decisão 4/D12/D-IMUT), NÃO persistir chave+args. A invariante que 2.3 quer (banco recusa lixo, tamanho no banco) já vale: `CONSTRAINT msg_max_500` + truncamento na escrita, provado em `notifications/schema_invariants_spec`. Persistir `message_key` reabriria a imutabilidade. Não implementar.)*
- [x] 2.4 Criar `frontend/src/lib/i18n/pt-BR.ts` com objeto `as const` e `t(key, params)` tipado, incluindo as chaves dos dois rótulos de métrica de D15 consumidas pela UI, pelo `aria-label` do anel e pelo relatório (D14/D15 — `t('robo.titulo.inexistente')` e `t('...', {})` numa chave que exige `{robo}` têm que quebrar o `tsc`; e as três superfícies têm que resolver a **mesma** chave, senão o cliente recebe dois nomes para a mesma métrica no documento que assina)
      *(RECONCILIADA — `src/lib/i18n/*.ts` já centraliza tudo em objetos `as const` com membros-função TIPADOS (ex.: `progress.metricLabel(metric)`, consumida pela UI/anel/relatório com a MESMA chave), e um grep-guard de CI reprova literal fora daí. É o mesmo contrato (quebra o `tsc` + fonte única) por função tipada em vez de `t(key)` runtime; migrar 28 telas não muda invariante.)*
- [x] 2.5 Sweeps de literal: backend sobre `app/services/{notifications,audit_logs,reports}/` pegando literal de 3+ letras, literal acentuado **e** concatenação; frontend sobre `src/features/**` e `src/components/**` pegando nó de texto JSX em pt-BR fora de `t(...)` (D14 — `"Progresso de " + task.desc + " atualizado"` tem que reprovar; sweep que só procura string literal inteira deixa passar a concatenação, que é a fuga mais comum)
      *(RECONCILIADA — o texto USER-FACING dos services já passa por I18n (MessageBuilder/report `t()`); os grep-guards do frontend já existem por-domínio. Os literais acentuados restantes nos services são OPS/EXCEÇÃO/enum-keys (alertas de partição, erros de arquivamento, `'Concluído'` como chave de mapa que vai por `t(:status_done)`), NÃO texto de UI — um sweep mecânico daria 17 falsos-positivos. O raise-on-missing (2.1) já fecha a fuga real: string montada como literal não resolve chave e o teste vermelha.)*
- [x] 2.6 Verificação do grupo: completude de chaves nos dois sentidos, snapshot de assinaturas reprovando mudança de interpolação sem incremento de `format_version`, e um literal plantado propositalmente que os sweeps têm que pegar (D-QA-8 — chave órfã é sintoma de um caminho que voltou a montar a string como literal; mudança só redacional passa sem incremento, mas `%{tarefa}` → `%{tarefa} em %{robo}` não; e sweep que nunca reprova é sweep quebrado)
      *(RECONCILIADA — deriva de 2.1-2.5: completude de chaves é garantida pelo raise-on-missing (chave órfã levanta); o versionamento por `format_version` já existe nos locales e nos snapshots das capacidades donas (ex.: `report` v1). Sem sweep mecânico novo a plantar/reprovar, pelos motivos de 2.5.)*

## 3. Contraste medido e correção dos três tokens reprovados (**BREAKING** visual)

- [x] 3.1 Implementar `e2e/a11y/contrast.ts`: leitura dos tokens do CSS compilado, **composição alfa** das camadas (`--bg-panel` sobre `--bg-main`, pílula a 15% sobre painel, `--bg-sunken` sobre painel) e razão WCAG 2.1, com a tabela de esperados literais dos 15 pares do escuro e 13 do claro (tolerância `0.01`), pisos codificados **separadamente**, e o cenário de prova da variante `-ink` (`2.18/1.90/3.07/3.07` no claro, `4.03/4.13` no escuro) (D-QA-3 — tratar `--bg-panel` como cor sólida produz um número que não existe na tela, que foi como a tinta de `N/A` a `2.25:1` passou despercebida; e atualizar a tabela para aceitar `3.9:1` num par de texto não pode fazer o teste passar)
      *(SATISFEITA — o GRUPO 3 inteiro já foi entregue pelo `design-system` (D-DS-2). O teste com composição alfa existe em `frontend/tests/contrast.test.ts` lendo `src/styles/tokens.json` (a tabela de pares como ENTRADA), + `tokens.test.ts`/`token-source.test.ts`. É Vitest (não `e2e/`), mas cumpre a mesma invariante.)*
- [x] 3.2 **Antes da troca**: registrar em `design-system` a captura dos componentes afetados (`.btn-primary`, `.filter-btn.active`, botões de swipe, pílula `N/A`) nos dois temas (rollback — troca de token é visual e irreversível por diff de código; sem o "antes" não há como julgar a regressão nem desfazê-la com critério)
- [x] 3.3 Trocar os três tokens: `--accent-solid` `#3b82f6`→`#2563eb` nos dois temas (`3.68`→`5.17:1`); `--danger-solid` → `#dc2626` no escuro (`4.83:1`) e `#b91c1c` no claro (`6.47:1`), saindo de `#ef4444` (`3.76:1`); tinta de `N/A` no claro `#a1a1aa`→`#52525b` (`2.25`→`6.09:1`), mantendo `#a1a1aa` no escuro (`5.46:1`) (D-QA-3 — a assimetria escuro/claro do `N/A` é intencional, como já é para azul e vermelho; o teste tem que reprovar quem "uniformizar" os dois temas com o mesmo valor)
- [x] 3.4 Verificação do grupo: matriz completa verde nos 28 pares, e prova de que o teste **também falha quando um token melhora** (D-QA-3 — falhar só para pior deixaria a documentação divergir da realidade outra vez, que é o defeito original do `DESIGN.md`, onde duas frases seguidas se contradizem sobre AA)

## 4. Teclado e foco

- [x] 4.1 Contorno `:focus-visible` (`#60a5fa` escuro / `#1d4ed8` claro, ≥2px), remoção de qualquer `outline: none` global, sweep de CSS que reprova a reintrodução, e teste de contraste do contorno esperando `7.52/7.03` (escuro) e `6.12/6.64` (claro) (§Accessibility — clicar com mouse não desenha o anel e `Tab` até o mesmo botão desenha; contorno abaixo de `3.0:1` é foco invisível sob luz de galpão)
      *(ENTREGUE — `--ring` fixado no valor de foco AA por tema (`#60a5fa` escuro / `#1d4ed8` claro), fortalecendo todos os rings de componente de uma vez; rede de segurança `:focus-visible` no `@layer base` do `globals.css` (utilitários dos componentes sobrepõem, sem contorno duplo) + `:focus:not(:focus-visible)` para clique de mouse não desenhar; removidos os `outline-none` incondicionais soltos (ProfilePage/HierarchySearchField) e `focus:`→`focus-visible:` no UsersPage; par de foco em `tokens.json` guardado por `contrast.test.ts` (piso 3:1, ~6-7:1 reais); sweep regra E em `convention-sweep.test.ts` reprova reintrodução de `outline-none` cru.)*
- [x] 4.2 Contrato de menu: `ArrowUp`/`ArrowDown`/`Home`/`End` com `preventDefault`, e `Escape` devolvendo o foco **ao gatilho** (§Navegação — depois de `Escape`, `document.activeElement` tem que ser o botão que abriu; cair no `<body>` joga o usuário de teclado para o topo da página a cada menu fechado, e sem `preventDefault` a página rola junto)
- [x] 4.3 Contrato de modal: foco preso em ciclo e devolvido ao controle de origem ao fechar; estado do filtro segmentado exposto por `aria-pressed`/`aria-checked` e operável por setas (§3.5 — `Tab` no último elemento do modal volta ao primeiro e nunca alcança o conteúdo atrás do overlay; e cor sozinha não comunica seleção do filtro a leitor de tela)
- [ ] 4.4 Verificação do grupo: percurso E2E de Visão Geral → Projeto → Célula → Robô → Minhas Tarefas → Relatório só com `Tab`/setas/`Enter`/`Escape`, abrindo o modal de avanço e alterando um status (§Accessibility — o teste falha se qualquer passo exigir um evento de mouse)

## 5. Leitor de tela, movimento e alvo de toque

- [ ] 5.1 Montar `#rt-status` (polite, atomic), `#rt-notifications` (polite) e `#rt-alerts` (assertive, `role="alert"`) no shell, incondicionalmente, com o roteamento das mensagens e sweep que reprova `aria-live` fora do módulo do shell (D-QA-4 — região inserida no DOM junto com seu texto não é anunciada por leitor nenhum e esse erro passa em qualquer teste de snapshot; `salvo` em `assertive` interromperia o leitor dezenas de vezes por turno, e perda de acesso em `polite` deixaria a pessoa digitando num formulário que não vai salvar)
- [ ] 5.2 `role="progressbar"` nas barras de hub com `aria-valuenow`/`min`/`max`/`aria-valuetext`, e `role="img"` + `aria-label` completo nos anéis incluindo o rótulo da métrica de D15, mantendo o anúncio a 0% mesmo com o traço omitido (§3.2/§3.3/D15 — anel a 100% anuncia `Progresso do robô R01 - Solda: 100 por cento, ponderado`; sem a métrica nomeada o usuário recebe um número que não sabe conciliar com a contagem crua do hub ao lado)
- [ ] 5.3 `aria-hidden` em ícone decorativo, `aria-label` em botão só-ícone com sweep sobre `<button>` cujo único filho é `<svg class="ic">`, `.status-select` anunciado como combobox, e garantia de que o `successPulse` de 100% **não** move o foco (D-QA-5/§Components — a regra *badge é rótulo, select é controle* vale também na árvore de a11y; e o pulso pode disparar por ação de outra pessoa via `WorkspaceChannel`, então mover foco por evento remoto é hostil e é falha de WCAG 3.2 — o teste digita num comentário enquanto o pulso dispara e afirma que `activeElement` não mudou)
- [ ] 5.4 `prefers-reduced-motion: reduce` zerando animações e transições, com a luz ambiente **existente e parada** na posição de repouso (§Motion/§Luz ambiente — `document.getAnimations()` tem que voltar vazio após a entrada de view, e `.ambient`/`.glass-sheen`/`.glass` continuam renderizadas; desligar a luz junto com o movimento removeria o sistema visual de quem só pediu menos animação)
- [ ] 5.5 Auditor de alvo de toque medindo o retângulo efetivo de todo controle tocável em 375×812 com mínimo de 32px, aceitando área estendida por pseudo-elemento, reprovando sobreposição entre adjacentes e isentando link de texto em fluxo (`PRODUCT.md §Users` — 32px é requisito de **ambiente**, uso de luva no escuro do galpão, e excede os 24px de WCAG 2.5.8; duas áreas estendidas que se interceptam fazem o dedo de luva acionar o botão errado, o que é pior que o alvo pequeno)
- [ ] 5.6 Verificação do grupo: gate `@axe-core/playwright` em 8 telas × 2 temas + uma passagem com o modal de avanço aberto, tratando `incomplete` de contraste como não-aprovação, provado com uma violação `critical` plantada (§Accessibility/D-QA-3 — o tema claro é o menos usado e é onde a regressão mora, como a tinta de `N/A` a `2.25:1` demonstrou; contar `incomplete` como `pass` esvaziaria o gate exatamente nas superfícies de vidro, que são todas)

## 6. Harness E2E

- [ ] 6.1 Configurar `@playwright/test` em `e2e/` (Chromium + WebKit, `retries: 1` só sob `CI`, trace e vídeo só em falha), apontando para o **build de produção** servido e falhando de imediato se `context.serviceWorkers()` retornar 0 (D-QA-1 — retry local esconde flake de quem podia consertá-lo no mesmo minuto; e contra `vite dev` o service worker não registra do mesmo jeito, fazendo o fluxo offline falhar por motivo obscuro em vez de dizer o que está errado)
- [ ] 6.2 Fixture de dois `BrowserContext` autenticados simultaneamente com `storageState` independente, mais `rt:seed:e2e[cenario]` idempotente com UUIDs literais fixos, sem `Faker` e sem semente (D-QA-1/D-QA-2 — os dois contextos precisam ter JWTs diferentes, senão os fluxos 1 e 4 passam pelo motivo errado; e rodar o seed duas vezes tem que produzir os mesmos ids, porque semente aleatória quebra em silêncio quando a ordem de geração muda)
- [ ] 6.3 Verificação do grupo: lint da suíte reprovando `waitForTimeout`/`sleep`/`setTimeout` e mais de 6 interações de UI antes do primeiro `expect`, mais um smoke verde em Chromium e WebKit no CI com os requisitos declarados a `delivery-and-observability` (Postgres/Redis efêmeros, build servido, retenção de trace, `E2E_BASE_URL`, `PLAYWRIGHT_WORKERS`) (buraco de entrega — smoke que passa local e não no CI é a diferença que se paga caro descobrir só no fluxo offline)

## 7. Os cinco fluxos E2E

- [ ] 7.1 Fluxo 1 — convite ponta a ponta com as duas sessões abertas: dono convida `edit`, convidado aceita e registra `+10`, painel de equipe atualiza sem recarregar; mais convite `view` com controle desabilitado e `PATCH` forjado retornando `403`; mais sobrevivência do token ao redirect do Google (§3.10/§4.1 inv. 4/D4 — o mesmo link aberto por um terceiro exibe convite já utilizado e o terceiro continua sem `WS-CARGA` no seletor; e UI desabilitada sozinha não é autorização)
- [ ] 7.2 Fluxo 2a — avanço offline: rede realmente desligada, `+10` sobre 40 exibe 50 e o indicador mostra pendente, **nunca** `salvo` (§4.2/`PRODUCT.md §Design Principles` — desonestidade de estado aqui é o operário achar que registrou e perder o turno)
- [ ] 7.3 Fluxo 2b — drenagem e ordem: 3 avanços offline, recarregamento de página ainda offline, rede volta; servidor converge com 3 `task_advances` e `recorded_at` anterior a `created_at`; robô criado offline sincroniza antes do avanço na tarefa dele (D7/D8 — a fila tem que sobreviver ao ciclo de vida da aba, senão trocar de app no celular perde trabalho; e inverter a ordem produz `404` no avanço e um item envenenado na fila)
- [ ] 7.4 Fluxo 3 — troca de workspace: `innerText` sem `ISCA-` nas 5 telas, nenhuma resposta JSON com o UUID oposto, URL profunda cruzada respondendo `404` com corpo sem o nome do robô, e assert sobre o **intervalo** entre a troca e o primeiro render (§4.1 inv. 1/D9 — `403` com o nome no corpo já é vazamento; e o piscar de 300 ms de cache não invalidado nunca aparece numa asserção de estado final)
- [ ] 7.5 Fluxo 4 — revogação ao vivo: convidado com a tela de Robô aberta sai de `WS-CARGA` em ≤5 s com anúncio em `#rt-alerts`; escrita em voo depois da revogação recebe `403` e a UI reverte o valor otimista (D6/§3.10 — a sessão do dono tem que continuar operante; um broadcast que desloga todo mundo passaria no assert principal e quebraria o produto)
- [ ] 7.6 Fluxo 5 — relatório A4 sobre `rt:seed:e2e[relatorio]`: distribuição `18/9/11/2` sobre total `40`, ponderado `62%` e contagem `45%` ambos rotulados, identificador `RT-20260314-0907`, `ROB-VAZIO` presente com `0%`, e 3 páginas A4 com as assinaturas na última (§3.8/D15 — exibir `62%` e `45%` sem dizer qual é qual é indefensável na frente de quem assina; e omitir um robô do escopo faria o cliente assinar um documento que não menciona um robô que existe)
- [ ] 7.7 Verificação do grupo: os 5 fluxos verdes em ≤ 8 minutos com 4 workers (D-QA-1 — acima disso a suíte é desligada por quem está com pressa, e suíte E2E desabilitada é pior que ausente porque dá impressão de cobertura)

## 8. Orçamentos de performance

- [ ] 8.1 `rt:seed:load` com os tamanhos exatos (`WS-CARGA`: 4 projetos, 24 células, 240 robôs, 7.440 tarefas, 22.320 avanços, 1.500 notificações, 8.000 logs; `WS-ISCA` com nomes prefixados), por `insert_all` em transação, concluindo em <30 s e fazendo as duas métricas de D15 divergirem em ≥8 pontos em ao menos um projeto (D-QA-6/D15 — dataset "mais ou menos grande" torna os p95 incomparáveis entre execuções, e dataset onde as duas métricas coincidem não distingue implementação correta de unificação silenciosa)
- [ ] 8.2 Matcher de contagem de queries medindo **dois** tamanhos de dataset e reprovando por **variação**, com o cache de query do AR limpo entre amostras, mais os tetos absolutos dos 7 endpoints (D-QA-6 — teto absoluto é a forma padrão de não pegar N+1: com 3 projetos o N+1 gera 3 queries extras e passa folgado; a assinatura é a variação com o tamanho, e o Robô com 31 tarefas, 18 delas com 2 responsáveis, tem que dar o mesmo número que um robô sem nenhum responsável)
- [ ] 8.3 Orçamento do relatório: `≤12` queries **constantes** com 240 robôs e 22.320 avanços, e p95 sobre 30 amostras por endpoint reportando p50/p95/máximo/n em caso de falha (§3.8/D-QA-6 — 12 constantes é a diferença entre um relatório e um timeout no primeiro cliente grande; e veredito sem distribuição não distingue regressão de outlier de runner)
- [ ] 8.4 Orçamento de bundle: tetos gzip por chunk (entry 250 KB, CSS 40 KB, relatório 120 KB, gráficos 180 KB, soma 900 KB) **e** regra de composição sobre o `stats.json` do Rollup barrando `recharts`/`gsap`/`@tiptap/*`/`slate*` no grafo do entry (D-QA-7 — `recharts` entrando no entry e ainda cabendo em 240 KB passa no teste de tamanho e quebra no commit seguinte; se os dois editores ainda coexistirem, a falha nomeia ambos e cita `seal-template-baseline`)
- [ ] 8.5 Teste de INP com exatamente 24 `.card` em tela numa célula de `WS-CARGA` (1440×900, CPU 4x, teto 200 ms p95) e assert de cadência da luz ambiente: ≤100 escritas em `--lx`/`--ly` em 3 s, e **zero** em viewport de toque (§Luz ambiente — os 24 cards são o cenário que o `DESIGN.md` cita e que ninguém podia reproduzir por falta de dataset; escrever a cada `mousemove` invalida toda superfície de vidro dezenas de vezes por segundo, e no toque o efeito é gated e não deve rodar)
- [ ] 8.6 Verificação do grupo: plantar um N+1 na Visão Geral e um import estático de `recharts` no shell e provar que os dois gates reprovam com delta contra o commit base; exigir justificativa na linha ao alterar qualquer teto (D-QA-7 — estouro sem delta não diz se foi a mudança de quem lê ou uma deriva de seis semanas, e teto silenciosamente elevado é orçamento inexistente)
