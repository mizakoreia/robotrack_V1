# robot-task-grouping

Categorias colapáveis na tela do robô (prefixo A./B./C., contagem, estado lembrado por robô) e exclusão em lote de tarefas por seleçãúo mçãltipla (owner-only, uma transaáôo, um recálculo por robá). "Apagar sem quebrar os clculos" j existe para tarefa individual — aqui entra o lote. Ver proposal/design/tasks/EXECUCAO + specs delta (robot-task-table, robot-tasks). Marcador: `git tag pre-task-grouping`.
