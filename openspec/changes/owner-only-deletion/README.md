# owner-only-deletion

Excluir projeto/celula/robo/tarefa passa a ser exclusivo do DONO. O papel
`edit` continua criando, editando, reordenando e registrando avanco — perde
apenas a exclusao. Travado na policy E no banco (gatilho no arquivamento).
