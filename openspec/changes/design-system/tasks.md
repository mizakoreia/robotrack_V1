# Tasks — `design-system`

Onda 0, sem dependências. Grupos 1–4 podem rodar antes de qualquer backend existir.
Grupo 8 (limpeza destrutiva) roda por último e tem backup imediatamente antes.

## 1. Tokens e temas

- [x] 1.1 Escrever o token set completo em `frontend/src/styles/globals.css`: escuro em `:root`, claro em `.light`, todos os papéis de `DESIGN.md` como triplas HSL sem alpha, **mantendo os nomes shadcn (`--primary`, `--background`, …) como aliases** dos novos papéis. (§5.1 — `Button.tsx`, `Card.tsx`, `Input.tsx` e `Tooltip.tsx` continuam compilando e renderizando sem serem tocados; se algum quebrar, o alias está faltando)
- [x] 1.2 Declarar as opacidades canônicas de superfície em `@layer components` (`.surface-nav` 0.82, `.surface-panel` 0.7, `.surface-menu` 0.97, `.surface-sunken` 0.28, `.surface-raised` 0.04 no escuro; valores do claro conforme a tabela de `DESIGN.md`). (§5.1 — o alpha não aparece em nenhum callsite; um `grep` por `hsl(var(--bg-` seguido de `/` fora de `globals.css` retorna vazio)
- [x] 1.3 Remapear `theme.extend` em `frontend/tailwind.config.js` para os papéis do RoboTrack, incluindo raio (`--r-xs`…`--r-pill`), sombras (`--sh-1/2/3`) e a grade de cards `repeat(auto-fill, minmax(260px, 1fr))`. (§5.1 — em 375px a grade mostra 1 coluna, em 768px mostra 2 e em 1440px mostra entre 3 e 5; se `minmax` estiver errado o breakpoint de 768px mostra 1 ou 3)
- [x] 1.4 **Verificação:** teste que lê os tokens computados nos dois temas e afirma os valores canônicos (`--bg-main` → `#0a0f1d` / `#f1f5f9`, `--text-main` → `#f8fafc` / `#0f172a`, `--accent` → `#3b82f6` / `#2563eb`), e afirma que `--bg-panel` é uma tripla de três componentes sem `rgba(` e sem `/`. (§5.1 — se alguém embutir alpha no token, o modificador de opacidade do Tailwind multiplica em cima e o teste pega antes de a tela sair torta)

## 2. As três variantes de status e o contraste medido

- [x] 2.1 Declarar as três variantes como três namespaces de token: cheia (`--success`/`--warning`/`--danger`/`--accent`/`--na`), tinta (`--*-ink`, valores do claro `#065f46`/`#92400e`/`#991b1b`/`#1e40af`/`#3f3f46` e do escuro `#34d399`/`#fbbf24`/`#f87171`/`#60a5fa`/`#d4d4d8`) e sólida (`--accent-solid` `#1d4ed8`, `--danger-solid` `#b91c1c`). (§5.1 — `#3b82f6` com branco em cima dá 3.68:1 e reprova AA; sem a sólida como token separado o botão primário nasce reprovado)
- [x] 2.2 Restringir cada namespace na config do Tailwind a um conjunto de propriedades: cheia só em `backgroundColor`/`borderColor`/`stroke`/`ringColor`, tinta só em `textColor`, sólida só em `backgroundColor`. (§5.1 — `className="text-success"` não gera regra CSS e o texto herda a cor do contexto, tornando o erro visível na primeira execução em vez de seis meses depois)
- [x] 2.3 Criar `frontend/src/styles/tokens.json` com a tabela de pares declarados (`{ fg, bg, alpha, tema, min }`) — 10 pares de pílula de status, 4 de base, 2 de sólida sobre branco — e implementar `frontend/tests/contrast.test.ts`, que compõe a pílula sobre a superfície com o alpha do papel, computa luminância relativa WCAG 2.1 e falha abaixo de 4.5:1 (corpo) / 3:1 (não-texto). (§5.1 — trocar `--text-muted` do claro de `#475569` para `#94a3b8` derruba o par de 6.92:1 para 2.28:1 e o teste falha nomeando par, tema, valor medido e mínimo)
- [x] 2.4 **Verificação:** teste de cobertura que varre as classes utilitárias de cor efetivamente emitidas pelo build e falha se alguma usar um token sem linha correspondente em `tokens.json`. (§5.1 — sem isso, um par novo entra na tela sem nunca passar pelo teste de 2.3 e o contraste medido vira ficção)

## 3. Tipografia e ícones

- [x] 3.1 Ligar o Inter 300–700 no `frontend/index.html` com `font-display: swap` e stack de fallback `Inter, system-ui, -apple-system, "Segoe UI", sans-serif`; declarar a escala fixa em rem (`.title` 1.65rem/700/−0.02em, `.modal-title` 1.22rem/600, `.panel-header` 0.92rem/600, corpo 14px, rótulos 0.68–0.78rem) e a classe utilitária de `font-variant-numeric: tabular-nums`. (§5.1 — um anel indo de `8%` a `88%` a `100%` mantém a mesma largura de caixa por contagem de caracteres, e com `fonts.googleapis.com` bloqueado o texto segue visível na fallback, sem período invisível)
- [x] 3.2 Criar o sprite SVG inline (`components/icons/sprite.tsx`) e o componente `Icon` (18/15/22px), com todos os `<symbol>` sem `stroke`/`fill` literal e `aria-hidden` por padrão. (§5.1 — `<Icon name="check"/>` dentro de um elemento com `color: #34d399` tem `stroke` computado `#34d399`; se algum `<symbol>` fixar cor, o chevron do `StatusSelect` para de herdar a tinta do status)
- [x] 3.3 **Verificação:** lint que varre `frontend/src/**/*.{ts,tsx}` por codepoints de `Emoji_Presentation` e falha o CI nomeando arquivo, linha e codepoint, com allow-list para o módulo de glifos do relatório A4 (`✓ ◐ ○ —`). (§5.1 — um `<span>✅ Concluído</span>` colado num badge falha com `U+2705`, em vez de virar o primeiro emoji que abre a porta para os próximos)

## 4. Empilhamento e política de tema

- [x] 4.1 Declarar os sete níveis de empilhamento como custom property e como `theme.extend.zIndex` (ambient 0, content 1, sticky 20, sidebar 30, dropdown 60, modal 90, login 200) e adicionar lint que proíbe `z-index` numérico literal fora de `globals.css` e classes `z-[N]` arbitrárias, sugerindo o nível semântico mais próximo. (§5.1 — `z-index: 999` falha o CI; sem a proibição o próximo conflito de empilhamento vira `9999` e a escala morre)
- [x] 4.2 Ajustar `frontend/src/stores/themeStore.ts` para iniciar em `dark` na ausência de `rt-theme`, persistir em `localStorage['rt-theme']`, alternar a classe `light` na raiz, sincronizar `<meta name="theme-color">` (`#0a0f1d` / `#f1f5f9`) e adicionar o script síncrono anti-FOUC no `<head>` do `index.html`. (§5.1 — primeiro acesso num dispositivo com sistema em modo claro abre no **escuro**, e recarregar com `rt-theme = "light"` não pinta nenhum frame com fundo `#0a0f1d`)
- [x] 4.3 **Verificação:** guarda de CI que faz `grep -rn "prefers-color-scheme" frontend/src frontend/index.html` e falha se houver qualquer ocorrência, em JS ou em `@media`. (§5.1 — protege a decisão de produto de um "conserto" bem-intencionado que entregaria tema claro para a maioria dos celulares corporativos, a pior combinação sob luz de galpão)

## 5. Primitivos de superfície e progresso

- [x] 5.1 Reescrever `components/ui/Card.tsx` sobre os papéis novos: `.entity-ic` em tom único de accent, título, `.card-meta` com o badge **em elemento irmão do título**, anel e rodapé em `mt-auto` com `h-full`; comentário inline citando o modo de falha. (§5.2 — dois cards lado a lado, um com título `"Robô 1"` e outro com `"Robô de solda ponto lateral direito — estação 07"`, têm `offsetTop` de anel idêntico e mesma `offsetHeight`)
- [x] 5.2 Implementar `components/ui/ProgressRing.tsx` como `<path>` SVG com `stroke-linecap: round`, **omitindo o path de progresso quando `value === 0`**, com `role="img"` + `aria-label`; comentário inline citando o modo de falha. (§5.2 — a 0% o DOM contém só o path do trilho; o teste falha explicitamente se existir um path com `stroke-dasharray: 0`, porque omitir não é o mesmo que zerar e o ponto arredondado sugere avanço inexistente)
- [x] 5.3 Implementar `components/ui/Hub.tsx` com rótulo pequeno sobre valor grande e barra que anima por `transform: scaleX()` com `transform-origin: left`, `role="progressbar"` + `aria-valuenow/min/max`; comentário inline citando o modo de falha. (§5.2 — a 45% o `transform` computado é `matrix(0.45, 0, 0, 1, 0, 0)` e a `width` computada é `100%` constante; se a `transition-property` contiver `width` o teste falha)
- [x] 5.4 **Verificação:** testes de renderização para 5.1–5.3 cobrindo os três modos de falha (desalinhamento por título longo, ponto no anel a 0%, animação por `width`). (§5.2 — cada teste precisa reprovar a implementação ingênua correspondente; um teste que passa nas duas versões não protege nada)

## 6. Primitivos de rótulo, controle e diálogo

- [x] 6.1 Implementar `components/ui/Badge.tsx` como pílula estática: fundo na variante cheia com o alpha do papel, texto em `--*-ink`, **sem** prop `chevron`, **sem** `onClick`, não focável. (§5.2 — passar `onClick` é erro de `tsc --noEmit`; um badge clicável é o começo da confusão que a regra "badge é rótulo, seletor é controle" existe para evitar)
- [x] 6.2 Implementar `components/ui/StatusSelect.tsx` como `<select>` nativo com `appearance: none`, chevron do sprite **obrigatório e não suprimível pelo tipo**, `pointer-events: none` e `padding-right` reservado, chevron herdando a tinta via `currentColor`; comentário inline citando o modo de falha. (§5.2 — não existe prop que remova o chevron, e um teste estrutural falha se `Badge` e `StatusSelect` do mesmo status renderizarem a mesma árvore de elementos — que é exatamente quando ninguém descobre que a pílula é clicável)
- [x] 6.3 Implementar `components/ui/Chip.tsx` (responsável / contribuinte / tag), estático por padrão, com variante removível cujo botão tem `aria-label` com o nome e alvo ≥ 32×32px. (§5.2 — chip não removível não recebe foco por Tab; se receber, uma lista de 12 responsáveis vira 12 paradas mortas na navegação por teclado)
- [x] 6.4 Implementar `components/ui/Modal.tsx` no nível `--z-modal`, com overlay em `backdrop-filter`, `.modal-bar`, `.modal-foot`, focus trap, `aria-hidden` no resto da árvore e Esc que devolve o foco ao gatilho. (§5.2 — após abrir pelo botão "Registrar avanço" e pressionar Esc, `document.activeElement` volta a ser aquele botão; sem isso o foco cai no `<body>` e quem navega por teclado recomeça do topo)
- [x] 6.5 Implementar `components/ui/SaveIndicator.tsx` (`saving`/`saved`/`error`) com ícone do sprite, tinta de status e `aria-live="polite"`. (§5.2 — o texto de `error` não afirma que houve gravação; um indicador que diz "salvo" quando falhou é exatamente a desonestidade de estado que `PRODUCT.md` proíbe)
- [x] 6.6 Implementar `components/ui/FilterBar.tsx` como controle segmentado em pílula, segmento ativo em `--accent-solid` + branco, com `role`/`aria-selected` e altura ≥ 32px. (§5.2 — o fundo do ativo é `#1d4ed8` (6.70:1 com branco); o teste falha se for `--accent` `#3b82f6`, que dá 3.68:1 e reprova AA)
- [x] 6.7 **Verificação:** `axe` em cada primitivo isolado nos dois temas, mais teste de tipo provando que `<Button><Icon name="trash"/></Button>` sem `aria-label` falha `tsc --noEmit`, e que `:focus-visible` é visível em todo controle após remover o `button { outline: none }` global. (§5.1 a11y — botão só-ícone sem nome acessível é o defeito mais comum e o mais invisível numa revisão visual)

## 7. Luz ambiente e motion

- [x] 7.1 Implementar `frontend/src/lib/ambient.ts`: registrar `--lx`/`--ly` com `@property` como `<length>`, escrever apenas no elemento raiz, com throttle de ~32ms. (§5.1 — 60 eventos de `pointermove` em 1000ms produzem no máximo 32 escritas; a 60fps cada superfície de vidro é invalidada duas vezes mais e a inércia da luz não esconde mais nada)
- [x] 7.2 Implementar as três camadas em `globals.css` — `.ambient` (halo fixo, dois radiais de accent, `--z-ambient`), `.glass-sheen::before` (só sidebar/topbar/hub/painéis/menus) e `.glass::after` (borda de 1px, gradiente + `mask-composite: exclude`, todas as superfícies) — com `background-attachment: fixed` nos gradientes, e `backdrop-filter` restrito a sidebar, topbar, menus e overlay de modal. (§5.1 — com o ponteiro em `(1200, 300)` duas superfícies em cantos opostos resolvem a partir do mesmo ponto, e o `backdrop-filter` computado de `.card` e do painel é `none`)
- [x] 7.3 Implementar as três degradações: gate `@media (hover: hover) and (pointer: fine)` sem registrar listener no toque; `prefers-reduced-motion: reduce` congelando a luz na posição de repouso **sem removê-la**; `data-glow="off"` desligando tudo, inclusive o halo. (§5.1 — sob movimento reduzido, mover o ponteiro não altera `--lx`/`--ly` mas as três camadas seguem pintadas; o teste falha se ficarem com `opacity: 0` ou `display: none`, porque isso muda a leitura das superfícies e não só o movimento)
- [x] 7.4 Declarar `viewEnter` (fade + 8px), `menuIn`, `modalPop`, `successPulse` e hover de card (`translateY(-3px)`) nos keyframes do Tailwind, com curvas de saída exponencial entre 150ms e 320ms, e zerar animações e transições globalmente sob `prefers-reduced-motion`. (§5.1 — nenhuma `cubic-bezier` tem componente y fora de `[0,1]`; bounce numa UI industrial contradiz a personalidade "técnico e confiável" de `PRODUCT.md`)
- [x] 7.5 **Verificação:** medir p50 de duração de frame com 24 cards em tela a 1x de throttle de CPU, com a luz ligada e com `data-glow="off"`, registrando os dois números no repositório; medir também que o contraste de `--text-main` sobre `--bg-panel` é idêntico nas duas condições.) *(p50 de frame é sensível a hardware (EXECUCAO decisão 6): o CI trava a parte determinística — throttle ≤32 escritas/1000ms e a garantia de que a `.ambient` NÃO altera token de texto/fundo, logo o contraste é idêntico com glow on/off; o p50 wall-clock é medido por quality-and-accessibility. (§5.1 — o p50 com a luz ligada precisa ser igual à linha de base; e se o contraste mudar com a luz, o efeito está carregando informação e vira requisito de leitura, não reforço)

## 8. Limpeza da dívida do template

- [ ] 8.1 **Backup:** criar `git tag pre-design-system-cleanup` no commit imediatamente anterior às remoções e verificar que `git show pre-design-system-cleanup:frontend/package.json` retorna o manifesto com Recharts, TipTap e Slate ainda presentes. (rollback — 8.2 e 8.3 apagam arquivo e desinstalam dependência; sem o tag a volta é reconstrução manual)
- [ ] 8.2 Remover `frontend/src/styles/tokens-campfire.css`, seus imports, e os aliases shadcn temporários criados em 1.1. (§5.1 — `tsc --noEmit` e o build passam; duas fontes de verdade para cor fazem o teste de 2.3 ler uma tabela enquanto a cascata resolve outra, e o contraste medido vira ficção)
- [ ] 8.3 Desinstalar `recharts`, `@tiptap/*` e `slate*` de `frontend/package.json`, remover o código morto que os importava, e adicionar guarda de CI que falha se eles — ou `@radix-ui/*` e `class-variance-authority` — reaparecerem no manifesto. (D-DS-7 — `tsc --noEmit` passa provando que nada restante os importa, e um `npm i recharts` distraído falha o CI; a única entrada de texto livre do produto é o comentário de avanço de menos de 100 caracteres em `<textarea>`)
- [ ] 8.4 **Verificação:** rodar a suíte completa desta capacidade (contraste, cobertura de tokens, emoji, z-index, `prefers-color-scheme`, dependências, primitivos, a11y) num CI limpo e registrar o resultado; abrir issue para `delivery-and-observability` com os requisitos de CSP (`fonts.googleapis.com` em `style-src`, `fonts.gstatic.com` em `font-src`, hash do script anti-FOUC em `script-src` — não `unsafe-inline`). (§5.1 — sem a CSP correta o Inter não carrega em produção e o script de 4.2 é bloqueado, devolvendo o flash de tema errado que ele existe para eliminar)
