import type { AuditText } from './audit'

// internationalization G4 — tradução EN do chrome do modal de auditoria.
export const auditTextEn: AuditText = {
  title: 'Audit log',
  subtitle: 'Most recent entries (up to 200). Immutable trail.',
  loading: 'Loading the log…',
  loadError: 'Could not load the audit log.',
  empty: 'No audit entries yet.',
  close: 'Close',
}
